# FE-20260411-09 · 建立 A 线最小 smoke 矩阵

## 1. 任务元数据

- 任务编号：`FE-20260411-09`
- Phase：`1B`
- owner：`frontend`
- 优先级：`P0`
- 状态：`completed`

## 2. 任务目标

- 冻结 A 线上线后的最小 smoke 验收矩阵。
- 用固定检查项覆盖 ProblemDetail 的高风险主链路。
- 避免每次上线后临场决定测什么。

## 3. 当前事实

- A 线虽然不迁移详情，但 build/minify/轻混淆仍可能影响深链、恢复、聊天与任务动作。
- `task1` 近期已新增初步需求工作区分区、状态逻辑 Mermaid、`+需求`、历史详情删除、全屏相关交互，并把状态逻辑升级为“按 entity 分组管理 + 新 entity 直写 + 既有 entity 分桶分步合并 + merge session 子任务状态显示”，同时引入折叠子卡、逐条转移摘要与 entity `details` lazy hydrate，属于高波动高风险面。
- 最新同步的 `main-v1` 又为 `task7` 增加了“业务流程完整性补齐”自动顺序执行链，新增 `e2ePrelimFvsCompletenessSessionsBlock`、`e2ePrelimFvsCompletenessAllDoneConfirmBlock` 与工作区 `e2eRequirementScenarioSupplementJson` 补齐卡，属于 A 线当前必须覆盖的新运行时风险面。
- 如果 smoke 项不固定，运行时证据就无法横向比较。
- A 线验收必须兼顾详情入口、状态恢复与关键交互。
- `FE-20260411-08` 已提供固定运行时证据模板，因此本卡需要输出可以直接嵌入该模板的固定 smoke 表。
- A 线当前风险面已经不止详情深链，还包括默认入口、登录 / 模型配置门禁链、共享静态资源，以及 `task1` / `task7` 的新增高风险交互。

## 4. In Scope

- 最小 smoke 用例集合。
- 用例执行顺序、成功判定与失败记录方式。
- 与运行时证据模板的对接关系。

## 5. Out Of Scope

- 全量回归测试。
- 自动化 E2E 脚本。
- B 线双渲染对照。

## 6. 前置依赖

- `FE-20260411-01`
- `FE-20260411-08`

## 7. 实施说明

1. 已定义固定 smoke 检查列表，覆盖入口、恢复、聊天、任务动作、侧栏、报告、首页与认证链。
2. 已为每个 smoke 项写明最小操作、成功标准与建议证据位。
3. 已把 smoke 表结构对齐 `FE-20260411-08`，可以直接落入运行时证据模板。
4. 已明确失败时如何回写到运行时证据和后续失败分级。
5. 已为 `task1` 与 `task7` 增加专项 smoke，分别覆盖初步需求工作区和端到端事务流补齐链路。

### 7.1 A 线最小 smoke 矩阵

| 编号 | smoke 项 | 最小操作 | 成功标准 | 证据建议 |
| --- | --- | --- | --- | --- |
| S01 | 详情深链打开 | 访问 `index.html?caseId=<sample_case_id>` | 页面进入 ProblemDetail 详情视图，无入口级报错 | 页面截图 + Network |
| S02 | 刷新恢复 | 在详情页刷新当前页面 | 当前案例仍正确打开，聊天区 / 工作区未跳错案例 | 刷新前后截图 |
| S03 | 聊天发送 | 在详情页发送 1 条普通消息 | 消息进入聊天流，发送按钮 / 输入框 / 结果区行为正常 | 聊天区截图 |
| S04 | 任务动作 | 执行 1 条最小任务动作（如 task start / confirm / task action） | 动作完成，无 JS error，工作区 / 聊天至少一处产生预期反馈 | 操作前后截图 |
| S05 | 历史侧栏 | 打开“沟通历史”侧栏并关闭 | 侧栏能打开、渲染、关闭，无结构错乱 | 侧栏截图 |
| S06 | 报告跳转 | 从详情页进入报告页或直接打开 `report.html` | 报告页可打开，核心资源命中正常 | 报告页截图 + Network |
| S07 | 默认入口 / 首页 | 访问无 `caseId` 的 `index.html` 或 `home.html` | 正确进入首页链路，首页资源命中完整 | 首页截图 + Network |
| S08 | 登录链路 | 打开 `login.html` | 登录页可打开，`config.js`、`auth-runtime.js`、`vue-auth-assets/*` 命中正常 | 登录页截图 + Network |
| S09 | 模型配置链路 | 打开 `model-config.html` | 模型配置页可打开，资源命中正常 | 页面截图 + Network |
| S10 | `task1` 工作区专项 | 打开 `task1` 工作区，切换 `view/json/历史详情`，并执行 1 条最新高风险交互 | 工作区 tab 正常，且 `+需求` / merge session 子任务状态块 / 状态逻辑 hydration / entity 折叠卡 / 历史详情删除 / entity 分桶分步合并中的至少 1 条行为正常 | `task1` 专项截图或录屏位 |
| S11 | `task7` 事务流补齐专项 | 打开 `task7` 工作区并触发或回放“业务流程完整性补齐”链路 | `校验流程完整性 Session 计划` 可自动顺序执行，`需求场景事务流补齐` 卡 `view/json` 正常，全部完成确认与历史侧栏记录正常 | `task7` 专项截图或录屏位 |

### 7.2 `task1` 专项 smoke 细化

| 子项 | 最低要求 |
| --- | --- |
| T1-01 | 初步需求工作区能正常打开，不空白、不报错 |
| T1-02 | `view / json / 历史详情` 至少切换一轮，面板显示与按钮状态正常 |
| T1-03 | 以下高风险交互至少验证 1 条：`+需求`、merge session 子任务状态显示、状态逻辑图 hydration、entity 折叠卡展开 / 收起、历史详情删除、状态逻辑按 entity 分桶分步合并结果 |

### 7.3 `task7` 专项 smoke 细化

| 子项 | 最低要求 |
| --- | --- |
| T7-01 | `校验流程完整性 Session 计划` 卡可正常打开，不空白、不报错 |
| T7-02 | `自动顺序执行` 后 session 状态能从待校验推进到进行中 / 已校验，不出现静默卡死 |
| T7-03 | `需求场景事务流补齐` 卡 `view/json` 至少切换一轮，面板显示正常 |
| T7-04 | `所有端到端事务流补齐已完成` 确认块与历史侧栏记录能正确落地 |

### 7.4 执行顺序建议

1. 先做 `S07`、`S08`、`S09`，确认入口与认证链资源完整。
2. 再做 `S01`、`S02`、`S03`，确认详情主链与恢复能力。
3. 然后做 `S04`、`S05`、`S06`、`S10`、`S11`，覆盖高风险业务与旁路页面。

### 7.5 失败记录格式

| 字段 | 说明 |
| --- | --- |
| smoke 编号 | 如 `S04` / `S10` / `S11` |
| 失败时间 | 统一记录时间戳 |
| 失败页面 | `index.html` / `home.html` / `report.html` 等 |
| 失败现象 | 入口打不开、资源 404、交互无响应、数据错位、Console error 等 |
| 初步归类 | deploy 错配 / 缓存错配 / build 问题 / 轻混淆问题 / 业务回归 |
| 是否阻断 | `yes/no` |
| 是否触发回滚 | `yes/no` |

### 7.6 与运行时证据模板的对接

| 本卡项 | `FE-20260411-08` 对应位置 |
| --- | --- |
| `S01`–`S11` | “最小 smoke 结果”表格 |
| 失败记录格式 | “异常与回滚”区块 |
| 证据建议 | “浏览器命中证据”与截图位 |

## 8. 交付物

- 本任务卡 §7.1 的 A 线最小 smoke 矩阵
- 本任务卡 §7.2–§7.4 的 `task1` / `task7` 专项 smoke 与执行顺序
- 本任务卡 §7.5–§7.6 的失败记录格式与运行时证据对接说明

## 9. 验收边界

### 9.1 完成结果

- smoke 矩阵至少覆盖详情深链、刷新恢复、聊天发送、至少一个任务动作、历史侧栏、报告跳转。
- smoke 矩阵还覆盖默认入口 / `home.html` / 登录后首页相关最小链路，不会只看详情深链。
- smoke 矩阵对 `task1` 已覆盖初步需求工作区的最新高风险链路，不再只停留在旧版 task 动作，且包含 merge session 子任务状态、状态逻辑按 entity 分桶分步合并、折叠卡或其结果回归。
- smoke 矩阵对 `task7` 已覆盖“业务流程完整性补齐”自动顺序、补齐卡 `view/json` 与完成确认链，不会漏掉当前最新的事务流补齐风险面。
- 每个检查项都有固定成功标准，可直接复用。

### 9.2 不算完成的情况

- smoke 项目不固定，仍靠执行人临场决定。
- 漏掉详情深链、刷新恢复或聊天发送等关键链路。
- 详情深链能开，但默认入口或首页相关资源坏掉却仍被判为通过。
- `task1` 新增的初步需求工作区、`+需求`、merge session 子任务状态、状态逻辑 entity 分组/折叠卡、状态逻辑按 entity 分桶分步合并或历史详情链路完全未纳入 smoke。
- `task7` 新增的业务流程完整性补齐 Session、需求场景事务流补齐卡或全部完成确认链完全未纳入 smoke。
- 只有标题列表，没有最小操作与成功判定。

## 10. 必备证据

- 文档或产物文件：smoke 矩阵、成功标准表、失败记录格式
- 命令或检查入口：
  - `FE-20260411-08` 运行时证据模板
  - `index.html?caseId=<sample_case_id>`、`home.html`、`login.html`、`model-config.html`、`report.html`
- 最小回归项：按矩阵执行一轮时能覆盖详情主链路，且至少包含一条 `task1` 初步需求工作区回归与一条 `task7` 事务流补齐链回归
- 若适用则写 diff / fixture / 运行时证据：后续真实发布时按 `S01`–`S11` 填写运行时证据表

## 11. 失败/停止条件

- smoke 项无法覆盖 A 线最主要风险面。
- 成功标准描述模糊，执行人无法一致判定。
- 矩阵与 deploy / 运行时证据模板口径冲突。
- 若无法提供 `task1` / `task7` 专项 smoke 或默认入口 / 登录链路 smoke，本卡不得宣称完成。

## 12. 回写位置

- 专项 `README.md` §8.4、§13
- `tasks/README.md`

## 13. 本次执行记录

- 2026-04-12：冻结了 `S01`–`S10` 十项最小 smoke，覆盖详情主链、首页 / 登录 / 模型配置链、报告页与 `task1` 高风险交互。
- 2026-04-12：把 smoke 矩阵与运行时证据模板对齐，后续执行人可直接填表留证。
- 2026-04-12：本卡关账，后续 `FE-20260411-11` 直接引用本卡结果形成验收记录。
- 2026-04-12：合并最新 `origin/main-v1` 后，补记 `S11` 与 `task7` 专项 smoke；当前 A 线最新风险面已扩展到“业务流程完整性补齐”自动顺序、补齐卡 `view/json` 与全部完成确认链。
