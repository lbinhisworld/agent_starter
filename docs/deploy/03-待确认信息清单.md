# do1_smart_cto 部署前待确认信息清单

在正式安装前，请你和运维一起确认下面这些信息。

## 1. 虚拟机信息

- 操作系统版本：
- 服务器 IP：
- SSH 端口：
- 登录用户：
- 是否可使用 `sudo`：
- 是否可访问公网：

## 2. 域名与证书

- 是否已有域名：
- 域名是什么：
- 是否本次一起上 HTTPS：
- HTTPS 证书由谁处理：

## 3. 数据库方案

- 使用 `MySQL 8`：
- 数据库是否和应用同机：
- 数据库用户名：
- 数据库密码：
- 数据库名是否固定为 `do1_smart_cto`：

## 4. 项目部署策略

- 部署目录是否使用 `/srv/do1_smart_cto`：
- 是否用 `pm2` 守护后端：
- 是否用 `nginx` 统一反向代理：
- 是否需要开通 80/443：
- 后端是否仅内网监听 `127.0.0.1:3000`：

## 5. 前端配置

- `VITE_BACKEND_API_BASE_URL`：
- `VITE_API_URL`：
- `VITE_VALUE_STREAM_API_URL`：
- 是否保留现有外部分析接口：

## 6. AI 相关配置

- `VITE_DEEPSEEK_API_KEY` 是否已准备：
- `VITE_DEEPSEEK_API_URL` 是否仍使用官方地址：
- `VITE_DEEPSEEK_MODEL` 是否仍使用 `deepseek-chat`：
- Key 是由你手工写入还是由运维注入：

## 7. 安装前最终确认

安装前请确认以下事项都为“是”：

- 虚拟机已创建
- SSH 可登录
- 软件安装权限已开通
- 部署目录已确认
- 数据库方案已确认
- 域名或 IP 已确认
- 前端配置值已确认
- AI Key 已确认
- 允许我按文档步骤执行安装
