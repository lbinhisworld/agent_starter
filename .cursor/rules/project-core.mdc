---
description: 项目核心规则与协作约定
alwaysApply: true
---

> [!CAUTION]
> 这是本项目最核心的规则，必须严格遵守！
## 1. 代理核心原则

### 1.1 安全第一

- **永远不要**自动运行可能造成数据丢失的命令（如 `rm -rf`、`git reset --hard`）
- **永远不要**修改 `.gitignore` 中已被忽略的敏感配置文件
- **永远不要**暴露 API 密钥、证书或其他敏感信息
- 对于破坏性操作，必须先向用户确认

### 1.2 最小化变更

- 优先进行**最小化的、增量式的修改**
- 不要重构未被要求修改的代码
- 不要"顺手"修复不相关的问题
- 保持 diff 尽可能小，便于 Code Review

### 1.3 理解优先

- 在编写代码前，**先理解现有代码的设计意图**
- 阅读相关文件，理解上下文
- 不确定时**主动询问**，而非猜测

### 1.4 语言规范

- **代码**：变量名、函数名、类名使用英文
- **注释**：使用中文
- **Commit Message**：使用中文描述
- **与用户交流**：使用中文

### 1.5 角色文档优先级（冻结）

- 若当前会话已按某角色的 `00-core.md` 完成身份初始化，则该角色文档中的**职责边界、可写范围、默认输出**优先于模型/平台的通用默认行为。
- 尤其当身份为 **`architect owner`** 时，默认目标是：**架构判断、任务拆分、风险审查、Task Contract、派工与验收口径**；不默认进入实现位。
- 若角色文档与通用“直接实现”倾向冲突，以角色文档为准；不得因为用户表达了推进意图，就跳过当前角色边界。

### 1.6 明确授权词门禁（冻结）

- 对 **`architect owner`**：只有当用户明确表达「**你直接改 / 你来实现 / 你亲自落代码 / 直接提交补丁**」或等价明确授权时，才可修改 `frontend/`、`frontend-vue/`、`backend/` 业务代码。
- 「**处理 / 清扫 / 看下 / 排查 / 收口 / 推进**」等词，默认解释为**分析、设计、审查或派工请求**，**不**视为亲自实现授权。
- 若 owner 获得明确授权并决定亲自实现，必须先在回复中声明「**本轮切换为执行模式**」，并先完成台账/文档更新，再改业务代码。

## 2. 分形文档系统（Fractal Doc System）

### 2.1 核心原则

1. **原子更新**：代码修改完成后，**立即**同步更新对应目录的 `AGENTS.md`
2. **递归触发链**：文件变更 -> 更新文件 Header 注释 -> 更新所属目录 `AGENTS.md` -> （若影响顶层架构）更新本文档
3. **分形自治**：确保在任意子目录下，AI 都能通过该目录的 `AGENTS.md`，重建局部上下文

### 2.2 三级文档结构

| 层级   | 位置                        | 职责                        | 更新触发时机                                 |
| ------ | --------------------------- | --------------------------- | -------------------------------------------- |
| **L1** | 根目录 `AGENTS.md` (本文档) | 全局协议 + 顶层架构         | 架构变化、顶级模块增删、技术栈变更           |
| **L2** | 子模块下的 `AGENTS.md`      | 地位、职责、约束、成员清单  | 文件增删/移动/重命名、公开接口变化、职责变化 |
| **L3** | 文件头部注释                | `INPUT/OUTPUT/POS/PROTOCOL` | 依赖变化、导出变化、职责变化                 |

### 2.3 L2 子模块模板

```markdown
# Folder: {当前模块路径}

## 地位

{地位}

## 职责

{职责}

## 约束

{约束}

## 成员清单

| 文件路径   | 简述   |
| ---------- | ------ |
| {文件路径} | {简述} |

**触发器**: 一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
```

### 2.4 L3 文件头部模板

对于核心业务文件，应包含以下头部注释：

```typescript
/**
 * [INPUT]: 依赖外部 {模块/文件}
 * [OUTPUT]: 对外部提供的能力
 * [POS]: 在系统中的地位
 *
 * [PROTOCOL]: 一旦本文件逻辑变更，必须同步更新此 Header 和所属目录的 AGENTS.md
 */
```

### 2.5 工作流

**正向循环**（代码 → 文档）：
代码修改完成后，依次检查并更新：

1. L3：更新文件头部注释
2. L2：检查子目录下的 AGENTS.md 是否需要更新
3. L1：检查根目录 AGENTS.md 是否需要更新

只有完成以上检查，任务才算“完成”。

**反向循环**（文档 → 代码）：
进入新目录编辑前：

1. 先读该目录的 AGENTS.md（如不存在则标记需创建）
2. 再读目标文件的 L3 头部（如缺失则先添加）
3. 理解上下文后再开始工作

### 2.6 禁止行为

**严重违规（FATAL）**：

- ❌ 修改代码后不检查文档一致性
- ❌ 删除/移动文件后不更新 L2 模块文档
- ❌ 创建新模块却不创建 L2 文档
- ❌ 忽略业务文件缺失的 L3 头部

**一般违规（SEVERE）**：

- ⚠️ L3 头部与代码实际不一致
- ⚠️ L2 缺少新增的成员文件
- ⚠️ L1 结构描述过时
- ⚠️ L2 中父级链接失效

